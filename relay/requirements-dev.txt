# 开发与测试依赖。服务器上不需要装这些。
-r requirements.txt
pytest>=8.0,<9.0
pytest-asyncio>=0.24,<1.0
httpx>=0.27,<1.0
# 浏览器冒烟测试（tests/test_ui_smoke.py）用它连 Chrome 的调试端口。
# uvicorn[standard] 会顺带装上，但那是运行依赖的副作用，不该当成测试依赖的来源。
websockets>=13,<18
